use_frameworks!

### Don't forget to use 'pod repo update' from time to time :) ###

# Specs Sources List
source 'https://github.com/CocoaPods/Specs.git'

# Project Configuration
project 'trexer.xcodeproj'
platform :ios, '13.5'

## Settings
inhibit_all_warnings!

plugin 'cocoapods-keys', {
:project => "trexer",
:keys => [
  "TrexerAPIClientKey",
  "TrexerAPIClientSecret"
]}

## App Target
target 'trexer' do
end
